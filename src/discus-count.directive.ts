// import { Directive, ElementRef, Input, Renderer } from '@angular/core';
// import { DisqusService } from './disqus.service';

// @Directive({ selector: '[disqusCount]' })

// export class DisqusDirective {
//     constructor(el: ElementRef, renderer: Renderer, dService: DisqusService) {
        // dService.getCount().subscribe((res)=>{

        // })
        // renderer.setElementProperty(el.nativeElement, 'textContent', '');
//     }
// }
